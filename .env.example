# Copy to .env and fill in. Never commit .env.

# Required for recipe generation. Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional. API server port (default 3001).
# PORT=3001

# Optional. Set to "true" for Mock Mode: sample recipes only, no backend or AI.
# Use for local testing without the API. AI recipe generation requires the backend in production.
# VITE_MOCK_MODE=false
